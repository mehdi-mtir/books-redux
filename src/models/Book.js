class Book{
  constructor(iban, title, date_publication, prix){
    this.iban = iban;
    this.title = title;
    this.date_publication = date_publication;
    this.prix = prix;
  }

}

export default Book;
